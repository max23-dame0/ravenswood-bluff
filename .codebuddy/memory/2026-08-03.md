# 2026-08-03

## 修复 GitHub CI lint-and-test ruff format 报错
- **根因**：最新提交 `fedf459`（拆分三上帝对象 facade）引入 3 处不符合 ruff format 规范的代码，CI `ruff format --check` 失败（3 files would be reformatted）。
- **修复**：对 3 个文件运行 `ruff format`：
  - `src/agents/ai_agent.py:283`（fence_match re.search 单行化）
  - `src/agents/ai_agent_delegation.py:327/457/619`（`_remember_critical_event`、`_mentioned_visible_names` 签名单行化、target_name 三元表达式）
  - `src/orchestrator/game_loop_delegation.py:370`（`_collect_nomination_intents` 签名单行化）
- **验证**：`ruff format --check src tests` = 140 files already formatted；`ruff check src tests` = All checks passed；`pytest tests -q` = 447 passed / 0 failed。
- 改动纯格式化，无逻辑变更。已提交推送（commit bd75018 + f87f76d 到 main）。

## 修复 CI 第二个报错：test_storyteller_judgement_logging.py 9 个失败（storyteller_run.log 不存在）
- **现象**：GitHub CI `pytest tests -q` 报 9 个失败，全部是 `Path("storyteller_run.log")` 不存在（exists=False / FileNotFoundError）。
- **根因（环境依赖假象）**：`storyteller_delegation.py:45` 的 `_ensure_storyteller_log_handler()` 在**模块导入时**执行，把 `storyteller` logger 的 FileHandler 绑定到**导入时 cwd**（CI=项目根）的 `storyteller_run.log`。测试用 `importlib.reload(storyteller_module)` 只重载 facade（`storyteller_agent.py`），**不递归重载** `storyteller_delegation`，所以 chdir 后 handler 仍指向项目根 → workspace 下永远无日志文件。本地通过是因为残留的历史 `storyteller_run.log`（tests/test_runs/ 及项目根 184MB 旧文件）"吃假文件"。
- **复现验证**：本地删残留文件 + HEAD 版本 → 复现失败（RC=1）；加修复 → 删残留后 RC=0。
- **修复**：`tests/test_orchestrator/test_storyteller_judgement_logging.py` 新增 `_bind_storyteller_log_handler(workspace)` helper（关闭/移除指向非当前 workspace 的 FileHandler，再调用 `storyteller_delegation._ensure_storyteller_log_handler()` 重建），11 处测试在 `monkeypatch.chdir(workspace)` 后调用。+32 行，1 文件。
- **验证**：删残留后单跑 21 passed；`pytest tests -q` = 447 passed RC=0；ruff check + format 全过。
- 残留的 `storyteller_run.log` 均被 .gitignore 忽略（`*.log`、`tests/test_runs/`）。

## 修复 CI pytest 过慢（卡 64% 一小时不动）
- **根因**：`tests/test_orchestrator/` 下 16 个「验收包装测试」通过 `subprocess.run` 调 `scripts/acceptance/*.py`（timeout=300），脚本内部又嵌套子进程（storyteller_acceptance 内部再跑 pytest + balance；balance 又跑完整 mock 对局；wave3 串行调 5 个兄弟脚本各 240s）。本地实测最慢 4 个：storyteller 59s / wave2 55s / alpha3 53s / balance 38s；CI 无 job 超时上限（默认 6h）→ 长时间挂起。
- **修复**：① 16 个验收测试文件加 `@pytest.mark.slow`（共 17 个测试）；② `pyproject.toml` 注册 `slow` marker；③ `ci.yml` pytest 步骤改 `-m "not slow"`（排除验收，快速单测 3.6s 跑完）+ job 加 `timeout-minutes: 25`。
- **验证**：`pytest tests -q -m "not slow"` = 3.6s RC=0；`-m slow` 收集 17 个；ruff check + format 全过；slow 测试单独运行正常。
- 验收覆盖未丢失：`scripts/acceptance/*.py` + `scripts/alpha1.1_acceptance.py` 仍为独立门禁可手动/单独执行。
