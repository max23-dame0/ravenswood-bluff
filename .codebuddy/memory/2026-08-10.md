# 2026-08-10

## ✅ 修复盲测样本泄露：样本按玩家分组连续排列（用户标注前发现）

- **用户反馈**：标注意义存疑 + "大部分能看出来是谁发言的，因为数据格式没整理好"，拒绝标注。
- **根因**：`scripts/export/export_blind_test_samples.py` 的 `export_samples` 按 `for pid in player_ids` 逐玩家 append 样本，且 sample_id 全局递增 → labeling.tsv 中 `_00~_05` 全是 P1、`_06~_11` 全是 P2…（30 条 5 玩家 × 6 条完全分组）。标注者靠"连续 6 条同一人 + 内容互证"即可识别身份，测的是内容可追踪性而非风格差异化。
- **修复**：新增 `_shuffle_samples(samples, seed)`（固定 seed 可复现），`export_samples` 在写文件前打乱样本顺序；用现有 30 条 live 样本（`data/blind_ready2/samples.json`）重新生成了打乱后的 `labeling.tsv`（P4/P3/P2/P5 交替，不再连续分组）。一次性重排脚本已清理。
- **单测**：`tests/test_benchmark/test_blind_test_samples.py` 新增 2 个（分组被打破 + 确定性），共 9 个；ruff 0（修了一个 B905 zip strict=False）+ 全量 `pytest -m "not slow"` 551 passed。
- **说明给用户**：盲测意义 = M3 验证真人能否凭发言风格区分不同 AI（>20% 随机基线）；修复后同一玩家发言已分散，但内容中互相引用（"P1 自称 厨师" 等）属游戏发言自然内容，无法也不应完全匿名化，标注者仍可综合内容判断——盲测本质是"综合风格+内容区分玩家"，只要不靠格式（序号分组）作弊即可。
- 未提交推送（待用户确认）。

## ✅ 二次修复：TSV 换行拆行（用户标注前发现"有几条没按格式排列"）

- **根因2**：3 条 live 发言内容含 `\n\n`（`72c6757f_13`/`23`/`25`），`export_samples` 写 labeling.tsv 时未清洗，TSV 以换行分行 → 一行被拆成多行、列错乱（检查发现 11 处异常行/列数 1~3）。
- **修复**：脚本新增 `_tsv_content()`（把 `\r\n`/`\n`/`\r`/`\t` 替换为空格），labeling.tsv 生成时应用。补 1 单测 `test_tsv_content_strips_newlines_and_tabs`。
- **验证**：重生成后 31 行（1 表头 + 30 样本）、异常行 0、无重复 sample_id；ruff 0 + 全量 `pytest -m "not slow"` 552 passed。
- **教训**：TSV 导出必须清洗单元格内的换行/tab（`_tsv_content`），否则内容含 `\n` 必然拆行——已固化进导出脚本。

## ✅ 治理 LLM 固定口头应答开场（用户反馈"行，""我先说个结论吧"机械）

- **统计确认**：盲测样本中"行，"开头 6/30（P1 一人 3 条），"行吧，"×1、"我先说句心里话，"×1——都是 LLM 主路径生成的固定口头应答，非 sanitizer 模板。
- **双层治理**：
  1. sanitizer 兜底：`speech_sanitizer.py` 新增 `_MECHANICAL_OPENERS` + `_strip_mechanical_opener()`（剥离句首固定词，保留实质内容；剩余 <4 字不裁剪；只剥句首不误伤句中），`sanitize_public_speech_content` 入口调用。
  2. prompt 引导：`common_rules.py` CORE_PRINCIPLES 沉浸式对话条款 + `difficulty_presets.py` standard speech_style_prompt 明确禁止"行，""行吧""我先说个结论""我先说句心里话"开场。
- **验证**：现有样本剥离命中 8/30 条（行，×6/行吧×1/我先说句心里话×1），剥离后自然连贯；新增 7 单测 `tests/test_agents/test_speech_sanitizer.py`（剥离/保留/短剩余/句中不误伤/入口应用）；ruff 0 + 全量 `pytest -m "not slow"` 559 passed。
- **说明**：盲测样本是修复前导出的旧数据，仍含机械开头；新样本需重新 live 导出才会应用剥离。

## ✅ 已提交推送（3 个原子 commit，`dcca11c..80fd280` main -> main）

- `c8a8e24` fix(blind-test): 样本打乱顺序 + TSV 换行清洗（导出脚本 + 3 单测）
- `def06af` fix(speech): 剥离 LLM 固定口头应答开场（sanitizer + common_rules + difficulty_presets + 7 单测）
- `80fd280` docs(memory): 盲测修复与机械开场治理记录
- 验证：ruff 0 + 全量 `pytest -m "not slow"` 559 passed。工作树 clean，main 与 origin 同步。
- 经验：PowerShell 下 `git commit` message 含中文+引号会解析失败，改用 `git commit -F <临时文件>` 提交。

## ✅ 代码审查改进：去硬编码（待用户确认提交）

- 用户要求"检查最近提交改动 + 避免硬编码"。审查 4 个 commit 后修复 4 点：
  1. `_MECHANICAL_OPENERS` 硬编码 → 拆为 `_DEFAULT_MECHANICAL_OPENERS` + `_mechanical_openers()`（支持 `BOTC_MECHANICAL_OPENERS` env 逗号分隔追加，项目 env 配置惯例）
  2. 死条目：`"行，那"` 永远匹配不到（`"行，"` 在前）→ 删除
  3. 魔法数字 `len(rest) >= 4` → `_MIN_OPENER_REMAINDER = 4` 常量
  4. `_shuffle_samples` 直接用对局 seed → `seed + _SHUFFLE_SEED_OFFSET` 解耦（避免换对局种子连带标注顺序变化）；函数内 import random 移到模块顶部
- **测试**：新增 env 追加/默认保留 2 测试；`test_shuffle_breaks_player_grouping` 的绝对断言（<5）过脆（seed+1 时恰好=5 失败）→ 改相对断言（< 分组排列一半 12）
- **验证**：ruff 0 + 全量 `pytest -m "not slow"` PASS（注意：本机 pytest 收尾清临时目录触发 safe-delete 拦截报 EXIT=1，用 `--basetemp tmp_work/pytest_tmp` 规避，实际测试全过）+ token_budget_benchmark PASS（common_rules 文本改动后全局层 1582 字符，缓存机制未破坏）
- 未提交（等确认）。
