# 2026-08-14

## PLN-043 全动作声明式工作流全量实施 + live 实测

用户要求：把认知工作流推广到全部动作（所有 agent 决策统一为声明式工作流）。**T1-T6 全部完成**：

**T1 决策原语化**（act() 重构，行为零变更）：提取 4 原语 `_decide_local_low_value` / `_decide_slayer_shot` / `_draft_reuse_decision` / `_decide_via_llm`（378 行内联 → 方法），act() 按原顺序调用——**696 既有测试零回归**（重构硬门禁）。

**T2-T4 动作工作流**：`src/agents/workflow/action_workflows.py`——8 动作类型 Workflow（recall→decide→validate→record），decide 复用原语（包装非重写）；`act()` 开头 `BOTC_WORKFLOW_ACTIONS=1` 路由（默认 off，失败回退原路径）；**观点演化闭环**：record 把决策 reasoning 回写观点库（有激活观点 update/supersede；无则创建软印象观点作演化起点，不 gate 不注入发言）。

**验证**：710 快速单测全绿 + ruff 0 + format 0 + doc health PASS + 10/10 gate + mock 8 人局开关 off 零 trace 新增（95 存量均为历史产物）/ 开关 on 26 trace 覆盖 4 动作。

**live 实测**（DeepSeek 5 人局 day_1，BOTC_WORKFLOW_ACTIONS=1）：
- 52 trace 覆盖 action_speak/action_vote/action_defense/action_nomination_intent，节点完整（recall/decide/validate/record）
- **观点演化闭环生效**：11 条观点全部由决策创建（decision_feedback）、6 条被后续决策更新
- fallback=0，决策全合法

**文档**：DECISIONS D019 + RPT-019 报告 + PLN-043 published + docs/README 注册 + PROGRESS 任务 26 ✅。

**踩坑**：
1. **record 只回写不创建**：初版无激活观点时跳过 → live 局（无认知 speak 开关）观点库恒空、闭环无源；修复为"决策创建软印象观点"。
2. **测试陷阱**：DummyBackend 返回 `{"foo":"bar"}` 先被 normalize 兜底（empty_speech），validate 单测须 monkeypatch `_decide_via_llm` 直接返回无 action 决策；nominate legal_targets 约束复杂，改用 vote 场景。
3. **测试污染**：某次 pytest BOTC_DATA_DIR 未隔离 → `agents/`、`storyteller/` 目录写入仓库根（wf-night-test trace），已清理。教训：测试必须显式设 BOTC_DATA_DIR=tmp。
4. **simulate_game**：默认 `--timeout-seconds 20` 机器负载高时 mock 8 人局会超时，加大到 120+。

**未提交改动**（等用户确认 commit）：ai_agent.py（原语化+路由）、action_workflows.py、test_action_workflows.py（14 测试）、文档（D019/PROGRESS/PLN-043/RPT-019/docs-README）、alpha-1.1-evidence 产物 ×1。工作区另有 `data/blind_ready3/`（不入库）。

## CR 审查提示词（同日第二轮）

用户要新开会话审查 PLN-043 未提交改动。已生成提示词落盘 `docs/reviews/cr-prompt-pln043-2026-08-14.md`（REV-010，已注册 docs/README REV 索引）。重点审查项：① **原语化等价性（最高优先）**——4 原语与 act() 原内联段逐行等价对照；② 包装非重写（decide 复用原语）；③ 开关语义默认 off + 失败回退；④ 观点演化闭环（更新/supersede/创建软印象、同 subject 不重复创建）；⑤ 信息隔离（快照排除阵营私密、reasoning 回写）；⑥ validate 假阴防护（DummyBackend 空 JSON 被 normalize 兜底，检查是否 monkeypatch）；⑦ 测试 BOTC_DATA_DIR 隔离（防 agents//storyteller/ 污染仓库根）。六项验收命令 + 交付物格式 + 红线（只审查不改代码/不提交）。

## CR 报告问题修复（同日第三轮，用户指示）

CR agent 产出报告 `docs/reviews/cr-review-pln043-2026-08-14.md`：**1 P0 + 4 P1 + 2 P2，全部修复完毕**：

- **P0-1（阻断）**：`_draft_reuse_decision` 提取时丢失 `and not refinement_mode` 条件（refinement_mode=True 本应走 LLM 精炼草稿，现在错误复用草稿——默认路径行为变更，且 696 测试无此组合断言未捕获）。修复：原语加 `refinement_mode` 参数恢复判断 + act()/工作流调用点传递；**补 2 个回归测试**（工作流路径 + act() 默认路径）。
- **P1-2**：工作流 `_decide` 漏猎手判定（act() 对所有 action_type 先过 `_decide_slayer_shot`，工作流误把 slayer_shot 当动作类型做 elif，是死代码）。修复：对齐 act() 顺序 local → slayer → draft → LLM。
- **P1-3**：recall 注入的观点摘要（strategic_thought）未传给 LLM 路径。修复：decide 调 `_decide_via_llm` 前合并进 llm_kwargs。
- **P1-4**：证据未匹配观点主题（同一 reasoning 盲目更新所有激活观点 + 证据按候选数重复计数）。修复：仅更新 `vp.subject_name in candidate_names` 的观点 + 单条证据；补回归测试。
- **P1-5**：`test_workflow_declares_four_nodes` 用独立 MonkeyPatch + Path(".") 污染 BOTC_DATA_DIR（残留环境变量致后续测试写仓库根）。修复：改用 pytest fixture；全量后根目录零污染。
- **P2-6**：玩家名硬编码 P1-P10。修复：从 visible_state.players 提取真实 player_id，**词边界正则精确匹配**（防 "p1" 误匹配 "p10"），display 用大写 player_id 与观点库约定一致。
- **P2-7**：无激活观点时只创建 candidates[0]。修复：为每个候选创建观点；补回归测试。

**修复后验证**：快速单测 **714 passed / 0 failed**（+4 测试）+ ruff 0 + format 0 + doc health PASS + 10/10 gate + mock 8 人局 game_over 零污染。CR 报告已追加 §6 修复记录 + 结论更新为可提交。**未提交**（等用户确认 commit）。

## CR 审查 + 验收（同日第三轮，报告 `docs/reviews/cr-review-pln043-2026-08-14.md`）

**结论：不可提交。** 发现 🔴P0×1 / 🟡P1×4 / 🔵P2×2。

**P0-1（阻断，默认路径行为变更）**：`_draft_reuse_decision` 原语提取时**丢失 `and not refinement_mode` 条件**（签名也无该参数，act() 调用点未传）。原 act() 草稿复用条件是 `cached_draft and action in {speak,defense} and not refinement_mode`；新原语只剩前两条件。→ refinement_mode=True 时错误复用草稿而非 LLM 精炼，违反 D019「行为零变更」。696 测试未覆盖「refinement_mode=True + cached_draft」组合（test_game_loop 只断言 speak_kwargs 值，用 ScriptedAgent 替身）。

**P1**：① `_decide` 节点用 `elif action_type=="slayer_shot"` 分支是死代码（orchestrator 从不以 slayer_shot 作 action_type 调 act()，它是决策返回值）→ 开关 on 时猎手 speak/nomination_intent 本地开枪判定丢失；② recall 写入 `ctx["strategic_thought"]` 但 decide 只传 `ctx["llm_kwargs"]` → 观点摘要未进 LLM prompt；③ `_feedback_decision_to_viewpoints` 有激活观点时对所有 active 观点用同一 reasoning 盲目提置信度（不按 subject 匹配），supersede 分支死代码（soft 不触发冲突）；④ `test_workflow_declares_four_nodes` 用 `_make_agent(Path("."), MonkeyPatch())` 独立 MonkeyPatch 不撤销 → `BOTC_DATA_DIR="."` 残留，同进程后续未隔离测试写仓库根 `agents/`、`storyteller/`（已清理）。

**P2**：玩家名硬编码 "P1"~"P10"（真实名 "Player 1" 不匹配）；无激活观点只取 candidates[0]。

**验收六项全 RC=0**：710 快速单测 / ruff 0 + format 246 / doc health PASS / alpha1.1 10/10 / mock 8 人局 game_over（**180s 首次 timeout 随机波动，300s 稳定**，零 trace 新增）/ 14 专项 + 开关 on 冒烟 26 个 action_* trace。工作区已恢复会话初始 git status（清根目录 agents/、storyteller/、m5l-040902.md）。
