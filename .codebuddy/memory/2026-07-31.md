# 2026-07-31 工作日志

## 环境搭建（Python 依赖 + 运行/测试环境）
- 系统 PATH 无 `python`（仅 Microsoft Store 重定向器）；用 `install_binary` 安装了受管 **Python 3.13.12**，路径：
  `C:\Users\maxdamema\.workbuddy\binaries\python\versions\3.13.12\python.exe`
  （后台有 3.14.3 在装，占用锁；故复用已装好的 3.13.12，满足 `requires-python >=3.11`）。
- 虚拟环境建于 `d:\ravenswood-bluff\.venv`（PowerShell 中 `python` 不在 PATH，需直接调用 `.\.venv\Scripts\python.exe` / `pip.exe`，可跳过 Activate.ps1 执行策略问题）。
- 依赖安装：`pip install -e ".[dev]"` 成功（默认 PyPI 可用，无需内网镜像）。pytest 9.1.1 / ruff 0.16.1。
- 验证：mock 后端 `pytest tests -q` 可跑；`ruff check src tests` 可跑；`BOTC_BACKEND=mock python -m src.api.server` 启动可达 http://127.0.0.1:8000。
- **既有问题（非环境搭建导致，超出本次范围）**：17 个测试失败 + 406 个 ruff 错误。典型如 `GameDataCollector._normalize_trace_entry() takes 1 positional argument but 2 were given`。如需清零需单独修复代码。

## 修复既有缺陷（环境就绪后，按 PROGRESS/DECISIONS/handoff 验收清单）
- **ruff 阶段一零告警**：`ruff check --fix --unsafe-fixes` 自动修复 372 处（W291/W292/W293/F401/F811/F541/F841/E701）；`ruff format` 归一 132 文件（覆盖 W291/W292/W293）；手动修复剩余 20 处 = F821(3) + E402(17)。F821 真 bug：`human_agent.py` 缺 `Optional` 导入、`night_phase.py` 在 `TYPE_CHECKING` 补 `PlayerState`。E402：`server.py`/`minions.py`/`townsfolk.py` 的延迟导入上移（test_vector_memory.py 的延迟导入因需在 `sys.modules` 注入假 numpy/faiss 之后，故在 pyproject 加 `per-file-ignores` 豁免而非移动）。
- **测试 17→0 失败**：
  - 根因 1：`data_collector.py` 的 `_normalize_trace_entry` 同时被 `@classmethod`+`@staticmethod` 装饰，调用 `cls._normalize_trace_entry(x)` 多传 `cls` → 移除 `@classmethod`。影响约 12 个验收脚本测试。
  - 根因 2：`test_vector_memory.py` 注入假 numpy/faiss 但 `VectorMemory` 模块级 import 在注入前已缓存（np=None），用 `importlib.reload` 强制重载解决。
  - 根因 3（Windows 专属）：验收脚本 `print(json.dumps(..., ensure_ascii=False))` 含 emoji/中文，子进程 stdout 管道默认 GBK 解码抛 `UnicodeDecodeError`。改为 `ensure_ascii=True`（纯 ASCII 输出，跨子进程链路零非 ASCII）。
- **9-gate 验收**：`scripts/alpha1.1_acceptance.py` 子进程 stdout 解码加 `encoding="utf-8", errors="replace"`，消除 Windows GBK 解码崩溃隐患。`pytest tests` 全绿（0 失败）；9-gate 7/9 稳定通过，**2 门禁为非确定性（mock 随机/质量启发式），属预存 flaky，非本次回归**。
- **未提交**：环境搭建（`.venv` 已被 `.gitignore` 忽略）+ 上述修复改动均在工作区，未 commit；PROGRESS 建议「format-only」单独提交，待用户决定。ruff 阶段二（I→UP→B→SIM）逐族启用尚未执行。

## 规范化整理 P0-P5 收尾验证（同日晚些）

- **P3 遗漏修复（关键坑，已入 DECISIONS D011）**：scripts 分目录时只同步了「聚合入口 → 叶子 gate」的路径，**漏了「叶子 gate → 兄弟 gate」的二级引用**，造成 9 个测试报 `can't open file ...\scripts\<name>.py`。教训：移动脚本后要 grep 的不止入口文件，任何含 `subprocess` / `run_script` 的脚本都要查。修复涉及 `wave1~4_acceptance.py`、`a3_memory_acceptance.py`、`ai_eval_acceptance.py`、`storyteller_acceptance.py`、`storyteller_balance_acceptance.py` 与 2 个测试文件。`run_script(name)` 的 `name` 现统一为「相对 `scripts/` 根」（如 `"acceptance/xxx.py"`、`"debug/xxx.py"`），docstring 已固化。
- **隐藏坑：silent skip 掩盖失败**。`tests/test_orchestrator/test_storyteller_balance_sample_export.py` 用 `_find_sample_export_script()` 找不到脚本就 `pytest.skip`，P3 移动后该测试被静默跳过，路径修好才暴露出真实的超时问题。后续新增 subprocess 测试应显式 fail 而非 skip。
- **lint 从 112 项收敛到 0**：F401(20)/F541(8) → `ruff check --fix`；E402(76) 全在 `scripts/**`，是 `sys.path.insert(REPO_ROOT)` bootstrap 后再 import 的预期模式 → per-file-ignores 而非改代码；F841(7)/E712(1) 手工清理。
- **`ruff format --check` 意外已全绿**（182 文件），因此原计划的「format-only 独立提交」不再需要，直接启用了 pre-commit `ruff-format` 钩子并移除 CI `continue-on-error`。
- **两处 `timeout=60` → `180`**：跑整局 mock 对局的用例在全量 pytest 并发下稳定超时（单跑 <20s），属时限过紧而非功能缺陷。
- **最终验证全绿**：`ruff check src tests scripts` 0 告警、`ruff format --check` 182 已归一、`pytest tests -q` **447 passed**（连跑两轮）、`python scripts/alpha1.1_acceptance.py` **exit=0（9/9）**。此前 PROGRESS 记的「9-gate 7/9 + 2 门禁 flaky」不再复现，原判断部分源于路径断链的连锁失败。
- **待办**：240 项改动仍未 commit，建议按 P0-P5 分 6 个 commit；ruff 阶段二规则待逐族启用；`check_doc_health.py` 待纳入 CI。

## 文档治理收尾（doc-governance 增强 + 健康核对）
- 清理上轮治理遗留临时脚本（`.codebuddy/tmp_*.ps1/.py` 与 `docs/.gov_body.md`）。
- 增强：`docs/README.md` §7 登记 29 个 `alpha-1.1-evidence/` 证据文件名（frontmatter 豁免）；新增 `scripts/check_doc_health.py`（frontmatter 必填字段 + 受控词表 + 死链校验；绝对路径为非致命告警、`--strict` 升级为失败）作 CI 门禁。
- 健康核对：AGENTS/CLAUDE/MEMORY/DECISIONS 路径均有效（`docs/` 已重组 plans/reference/releases/reviews/，参考文档在 `docs/reference/`）；DECISIONS 补 D011（文档治理规范：单一语料根 + frontmatter 标准 + 证据豁免）。
- 修复：历史文档中原先指向不存在的 `鸦木布拉夫小镇` 路径的绝对链接，被一次批量脚本误清空为 `]()` 后已用相对路径修复（约 61 处空链接→相对链接，含 `game_loop.py`/`server.py` 等源码链接与中文描述性文档链接）。剩余历史 task board 中的绝对路径链接按 D010 保留为基线（CI 仅告警、不失败）。
- 教训：PowerShell 5.1 读 `.ps1` 按 ANSI，含中文会乱码致解析失败——脚本避免中文或显式写 BOM；相对链接改写用「文件深度 + 仓库相对路径」手动计算，勿用 `System.IO.Path.GetRelativePath`（本机 .NET Framework 无此方法）。

## 文档健康门禁收尾（check_doc_health.py 33 死链清零）
- 运行 `scripts/check_doc_health.py` 发现 33 处 dead link 违规 + 11 处绝对路径告警。经核验，全部 33 处死链的目标文件真实存在，仅相对路径写错（子目录/层级错误），已逐一修正为有效目标：
  - `docs/plans/alpha-1.2-plan.md` 的 `../plans/lan_play_guide.md` → `../guides/lan_play_guide.md`
  - `alpha-0.2-plan/` 下 `automated-acceptance-and-testing-plan.md` → `acceptance-and-testing-plan.md`；`role-implementation...` 的 `../../rule_matrix.md` → `../../reference/rule_matrix.md`；`acceptance-and-testing-plan.md` 的 `../../releases/{validation_report,frontend_acceptance}.md` → `../../reviews/...` / `../../frontend_acceptance.md`
  - `wave-4-task-board.md` 的 `../../../tests/test_*.py` 补子目录（`test_state`/`test_orchestrator`/`test_agents`）
  - `reviews/alpha-0.1-release-notes.md` 的 `../rule_matrix.md` → `../reference/rule_matrix.md`；`../releases/{validation_report,frontend_acceptance}.md` → `../reviews/...` / `../frontend_acceptance.md`
  - `reviews/GEMINI_*` 的 `../../src|tests/...` 修正层级与子目录（`src/engine/phase_manager.py`、`src/agents/memory/working_memory.py`、`tests/{test_orchestrator,test_agents,test_engine}/...`）；`../plans/alpha-0.2-plan/HANDOVER_ALPHA_0.2.md` → `HANDOVER_ALPHA_0.2.md`
- 另将 10 处仓库内绝对链接（`d:/鸦木布拉夫小镇/scripts/...`、`file:///.../docs/lan-play-guide.md`）改为相对路径（`../../../scripts/acceptance/...`、`../guides/lan_play_guide.md`）。
- 结果：`check_doc_health.py` 退出码 0，0 违规，仅剩 1 处无害告警——`reviews/GEMINI_HANDOFF_REVIEW_2026-04-18.md:16` 指向个人机 `c:/Users/Administrator/.../handoff_document.md.resolved`（无仓库等价物，按 D010 历史基线保留为告警）。清理临时产物 `.codebuddy/tmp_ci_out.txt`。

## 规范化整理 P0-P5 分阶段提交（中文+英文双版 message）
用户要求「分阶段提交，消息中英文双版」。先 `git reset` 取消全部暂存（保留工作树），再按阶段 `git add -A <dir>` 分组提交，全程未推送（push 需用户另行确认）。7 个 commit（新→旧）：
1. `2f68717 docs:` 文档体系重组为 plans/reference/releases/reviews/guides 五类 — Reorganize docs into five categories...
2. `c463ff3 scripts:` 验收/基准/导出/调试脚本归位至子目录 — Move gate/benchmark/export/debug scripts... + 修复 9 处二级引用
3. `cd56e3c tests:` 测试按被测模块归位至子目录并统一替身 — Relocate tests into subdirs; unify doubles
4. `0995f15 refactor(agents):` persona 模块归属微调与全模块 lint 规范化
5. `1615dba chore:` 清理根目录散落脚本，仅保留 simulate_game.py — Remove debug_imports.py/read_log.py
6. `3b3f29e build:` 补全工程配置与项目治理文档 — ruff per-file-ignores + 启用 ruff-format 门禁 + AGENTS/DECISIONS/PROGRESS
7. `40b90f9 refactor:` 引擎/编排/API/LLM 模块 lint 规范化
- **未纳入提交**（保持未提交）：运行时产物 `data/games.db`、`runtime_game_logs/*`；agent 环境目录 `.codebuddy/`（含 skills/ 大目录，非项目源码）。
- 注：分阶段提交中间态因重命名会短暂破坏跨引用（不可单独 build），最终态与已验证基线一致（447 passed / 9-gate exit 0）。`git push` 待用户确认。
