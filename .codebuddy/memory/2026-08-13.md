# 2026-08-13

## D017 验收 flaky 根因修复（续 2026-08-12 PLN-041 会话）

用户决策：A. 保留 ruff 0.16.1 格式改动（与本地版本对齐，format --check 通过）；另要求解决全量 slow 6 项验收失败。

**根因定位**（非 mock 噪声，是生产行为缺陷）：
- 6 项失败 = 2 个根因，均指向「archetype 差异在投票行为中不可见」：
  1. `aggressive_vote_push_rate 1.0 <= 1.0`（long_game_ai / wave3 / a3_memory / alpha3 4 项）
  2. `persona_diversity_score 0.2`（ai_evaluation 2 项）
- 机制：vote 走本地判定路径（`_should_use_local_low_value_action`），suspicion 常落 threshold±margin(0.06) 模糊带 → `DecisionEngine.persona_vote_bias` 兜底；该函数只看 `decision_style` 文案（`refresh_persona_profile` 用 `_pick_stable` 随机 pick，与 archetype 弱相关）→ aggressive/silent 全投 yes、签名趋同。
- 诊断脚本：`tmp_work/diag_vote.py`（按 archetype 打印 suspicion/threshold/vote）。

**修复**：`persona_vote_bias` good 分支先按 `archetype.assertiveness`（`persona_profile["archetype"]`，Archetype 实例）定倾向：high（aggressive/paranoid/strategist）→ yes；low（silent/cooperative/protector/outsider_vibe）→ no；neutral 或无 archetype 回退原文案。evil 分支不动（保持既有行为）。

**验证**：全量 **676（含 slow）/ 0 failed / 0 errors**（254s）+ ruff check 0 + format 0 + `alpha1.1_acceptance.py` 10/10 + mock 8 人局 game_over。

**文档**：DECISIONS D017；PROGRESS 验证状态表（676 全绿）+ 任务 24 更新 + 会话记录；MEMORY.md Auto Memory（修正 2026-08-12 的"基线 flaky"表述为已修复）。

**未提交改动**（等用户确认 commit）：PLN-041 全部（retrieval/workflow/rule_knowledge + 测试 + gate）+ D017（decision_engine.py）+ 文档治理 + ruff 0.16.1 格式改动（14 个存量文件，用户已确认保留）。

## PLN-042 认知工作流全量实施 + live 实测（同日第三轮）

用户要求：把认知工作流方案写成 PLN-042 计划文档 → SpecForge TDD 逐项实施 → 严格验收 → live 实测确保实际效果。**全部完成**：

**代码（T1-T4，35 新测试）**：
- `src/agents/reasoning/viewpoint.py`：Evidence（hard/soft）/ Viewpoint / ViewpointStore（viewpoints.jsonl 仅 live 落盘）
- `src/agents/reasoning/viewpoint_engine.py`：确定性置信度（0.35+0.15*hard+0.06*soft，封顶 0.95）/ 门控 0.45 / 强断言降级 / 冲突 supersede
- `src/agents/workflow/cognitive_workflow.py`：recall→reason→speak→record 工作流（全确定性）
- `AIAgent.act()` 认知块（**关键坑：orchestrator 全走 act()，挂 act_with_strategy 不生效**）+ build_memory_snapshot + 观点摘要注入 user 段；开关 BOTC_COGNITIVE_SPEAK 默认 off

**验证**：快速单测 **692** 全绿 + ruff 0 + format 0 + doc health PASS + mock 8 人局 game_over（viewpoints 零污染）；10/10 gate（跑完确认）。

**live 实测（DeepSeek 5 人局 day_1，BOTC_COGNITIVE_SPEAK=1）**：
- viewpoints 5 玩家落盘（p1 poisoner 无证据不强行断言——符合设计）；硬证据 0.95 vs 软印象 0.59-0.65 分级正确
- fallback=0
- **A/B 对比（同局开关开/关）**：开启=论证式发言（依据+前人说辞+隐藏占卜师底牌）；关闭=断言式（直接亮底牌"我昨晚查了一手"）——愿景落地实证

**文档**：DECISIONS D018 + RPT-018 报告 + PLN-042 published + docs/README 注册 + PROGRESS 任务 25 ✅。

**踩坑**：① pytest-asyncio auto 未生效需显式 marker + await；② safe-delete 拦截 basetemp（>50 文件）→ 每次唯一 basetemp（时间戳）；③ simulate_game 用 `--backend live` 参数 + `--timeout-seconds 600`；④ PowerShell 中文内联乱码 → 文件 + `-X utf8`。

## CR + 测试验收提示词（同日第四轮）

用户要新开会话做 CR 和测试验收。已生成提示词落盘 `docs/reviews/cr-prompt-pln041-042-2026-08-13.md`（REV-009，已注册 docs/README REV 索引）。包含：上班必读清单（AGENTS/DECISIONS D016-018/PROGRESS/PLN-041-042/RPT-017-018/rules）、待审查改动范围（retrieval/workflow/reasoning/rule_knowledge/ai_agent/decision_engine/文档/格式归一）、8 条硬约束红线核对项、六项测试验收命令（含 safe-delete 规避、mock 零污染检查、live 可选）、交付物格式（CR 报告 + 验收附录 + P0 停止规则）、红线（只审查不改代码/不提交）。

## CR P1 修复 + 分组提交（同日第五轮，用户指示）

CR agent 产出报告 `docs/reviews/cr-review-pln041-042-2026-08-13.md`（无 P0，3 P1 + 10 P2）。用户要求按报告修复 + 重跑 test_viewpoint_engine.py + 修正 RPT-018"硬证据 0.95"表述 + 分组原子提交。**全部完成**：

**P1 修复**（3 项）：
1. **P1-1 证据提取双重循环**（viewpoint_engine.py）：`for source in HARD_SOURCES: for text in hard` 导致一条文本被 8 个来源名重复标记 → hard 虚增 ×8、单条硬证据直接封顶 0.95、source 失真。修复：去外循环，source 统一 `"hard_memory"`；测试改 `==` 精确断言 + 单条硬证据置信度 ∈[0.4,0.6] 断言。
2. **P1-2 门控语义失守**：`compute_confidence(0,2)=0.47≥0.45`，≥2 条软印象可越门控。修复：`passes_gate` 改为 `hard_count>=1`（软印象无论置信度数值一律拦截）；docstring/D018/PLN-042/RPT-018 同步。
3. **P1-3 BOTC_VIEWPOINTS=1 被架空**：store 创建条件只看 cognitive_speak_enabled。修复：改为 `viewpoint_enabled()`（BOTC_VIEWPOINTS 强制分支生效）；补集成测试。

**验证**：test_viewpoint_engine.py **13 passed**（用户指定重跑）；快速全量 **696 全绿**；ruff 0 + format 0 + doc health PASS。

**文档修正**：RPT-018 ②"硬证据 0.95"改为"初版 0.95 为 P1-1 bug 虚高效应，修复后单条 ~0.52、两条 ~0.69"；D018 决策/原因/约束同步（门控 hard_count 语义 + 0.95 修正注 + 基线 696）；PLN-042 T2 行 + 红线 3 同步。

**4 组原子提交**（`c645af3..86008dc`，未 push）：
1. `c645af3` feat(reasoning)：观点-证据模型 + 确定性证据引擎（T1-T2，含 P1-1/P1-2 修复，22 测试）
2. `0258962` feat(workflow)：认知工作流四节点（T3）
3. `f0268ef` feat(agent)：AIAgent 集成（T4，含 P1-3 修复）
4. `86008dc` docs(pln042)：计划/报告/决策/CR 文档收尾（14 文件）

工作区 clean（仅 `data/blind_ready3/` 本地产物不入库）。**未 push**（用户本轮只说"提交"，推送待确认）。

## 报告 + 分组原子提交推送（同日第二轮）

用户要求写详细报告并分组原子提交推送。已完成：

**报告**：`docs/alpha-1.2-evidence/pln041-workflow-rag-report-2026-08-13.md`（RPT-017，7 章：背景/可行性核查/实施内容/验证结果/踩坑/提交状态/后续建议），已注册 docs/README。

**8 组原子提交**（全部推送 origin/main，`2897527..3cc55c5`）：
1. `3a19be3` feat(retrieval): BM25/混合检索基础设施（T1-T7）
2. `ff555a1` feat(workflow): DSL/执行器 + 说书人试点 + 行动轨迹（T11-T15）
3. `baa35cf` feat(agent): 规则书静态注入（T9，防幻觉核心）
4. `eef2207` feat(gate): 检索质量评测 + 工作流 gate（T10/T16）
5. `4476f9a` fix(engine): 兜底发言净化 + persona_vote_bias archetype 维度（D017）
6. `1543cea` docs(pln041): 计划 published + D016/D017 + RPT-017 登记
7. `c1b4f81` chore(memory): 工作记忆 + session-handoff
8. `3cc55c5` chore(ruff): 14 个存量文件格式归一 0.16.1

**教训（PowerShell 中文参数）**：`git commit -m "中文"` 在 PowerShell 下会 GBK 乱码/解析失败——统一改用 `write_to_file` 写 UTF-8 message 文件 + `git commit -F file`；中文文件名 add 用目录级（`git add docs/plans/`）。组 1 的乱码 message 已 amend 修正（未 push 前）。

工作区状态：clean（仅剩 `data/blind_ready3/` 本地产物不入库）。

## CR 审查 + 六项验收（同日第五轮，本会话）

用户以提示词启动独立 CR+验收会话（只审查不改代码）。结果：**可提交，无 P0**。

- 六项验收全过：① 快速单测 692 passed/0 failed（pytest 9.1.1 `-q` 不打印 summary，用 `-rA` 行统计 + RC）；② ruff check 0 + format 0（244 files）；③ doc health PASS（86 md，1 历史非致命 warning）；④ 聚合门禁 10/10；⑤ mock 8 人局 game_over（winner=evil, rounds=3）+ 零污染实测（局后 14:40 起无新增 viewpoints/action_trace，现存 4 个 viewpoints 为 12:57-12:58 live 历史）；⑥ 认知专项 4 文件 35+ PASSED。
- 硬约束 8 条逐条核对全过（不可变/隔离/前缀缓存/说书人确定性/开关/引擎/检索/边界）。
- **3 项 P1**（均在默认关闭的认知试点路径，不阻断）：① `viewpoint_engine.extract_evidence` 双重循环——`for source in HARD_SOURCES: for text in memory["hard"]` 把 1 条 hard 文本复制 8 条证据，置信度虚高直封 0.95，source 元数据失真，测试 `>=` 断言掩盖；② 门控失效——compute_confidence(0,2)=0.47≥0.45，≥2 条软印象即通过门控，强断言不降级，与 D018 宣称矛盾；③ BOTC_VIEWPOINTS=1 单独开不生效（store 创建条件只看 cognitive_speak_enabled）。
- 10 项 P2（BM25-only 门控虚设/英文名提取失败/subject_id=unknown/objective 关键词过滤脆弱/trace 文件膨胀等，详见报告）。
- 交付：`docs/reviews/cr-review-pln041-042-2026-08-13.md`（已注册待确认 docs/README）。建议 PLN-042 commit 时随修 P1。MEMORY.md 已精简至 ~90 行（旧 2026-07-31 治理细节已固化于 DECISIONS D007-D011，删减 Auto Memory 历史）。
