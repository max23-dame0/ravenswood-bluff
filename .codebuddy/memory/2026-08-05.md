# 2026-08-05

- **提交推送完成（7 个原子提交 → origin/main）**：① ec1e945 feat(agents) 白天发言按座次+草稿方案B（day_discussion.py、test_game_loop.py）；② 15bb4aa fix(agents) 提名修复（prompt_factory.py、decision_engine.py）；③ 2c8f64b feat(web) 游戏结束按钮+JSON清洗（public/index.html）；④ 593ad78 fix(agents) 邪恶频道清洗（evil_strategy.py）；⑤ ac882da feat(agents) 深度思考分级+per-player落盘+scavenge+max_tokens/全局层调优（ai_agent/base_backend/openai_backend/action_tool_registry/common_rules，.env 被 gitignore 忽略未入库）；⑥ bd82c57 test 更新；⑦ 0185ed6 chore（.gitignore+memory）。工作区 clean。

- **max_tokens 再提高 + 实测确认清零（完成）**：用户要求"多提高一点再跑一局确认"。nomination_intent 2000→**2400**、speak/defense_speech 1600→**2000**、vote/nominate 1200→**1600**、night_action 1000→**1400**、think 800→**1000**。确认局（5人 day_1，116.5s，67 calls）**fallback 清零**（0/35，真空输出 0）。实测 reasoning：vote **1151**（预算1600）、speak 425（预算2000），均在预算内。命中率 58.63%、output 15,658（reasoning 9,861 占 63%）、总 token 205,170、计费≈0.12 CNY。测试断言更新（speak 2000），test_agents+test_llm+test_orchestrator 全过、lint 0。**max_tokens 定稿**：nomination_intent 2400/speak/defense 2000/vote/nominate 1600/night_action 1400/think 1000。

- **max_tokens 再次调回安全值（实测收敛值不够）**：收敛后跑验证局（5人 day_1，113.9s，70 calls，命中率 54.4%）**3 个真空输出复现**（finish=length，reasoning 恰好=上限 1000/800/1200）。根因=DeepSeek thinking 长度波动大（nomination_intent 835~1200、speak 270~1000、vote 可达 800），收敛值余量不足。已调回：nomination_intent **2000**、speak/defense_speech **1600**、vote/nominate **1200**、night_action **1000**、think **800**（按实测峰值留 1.5-2 倍余量，优先清零 fallback）。⚠️ 权衡：调大后 reasoning 可能继续增长（模型思考更充分），output token 成本会更高；若成本不可接受，可选 AI_THINKING_LEVEL=low 降档或对波动大的动作单独降 effort。测试断言同步更新（speak max_tokens 1600）。测试全过、lint 0。是否清零待下一局 live 确认。

- **全局层精简 + max_tokens 收敛（用户确认执行）**：① 全局层（common_rules.py）精简 1616→**1527 字符**（COMMON_RULES_PREFIX 250→238、CORE_PRINCIPLES 647→恢复精简版、OUTPUT_FORMAT 304→273；语义保留，保密欺骗 CRITICAL 保留完整）。⚠️ 第一次精简到 1388 **低于 1500 测试断言**（test_agent_tools.py:134 `>=1500`），已恢复 CORE_PRINCIPLES 关键细节到 1527。② max_tokens 按实测收敛：nomination_intent 1500→**1200**（实测 completion 峰值 835）、speak/defense 1200→**1000**（实测 498）、vote 1000→**800**、nominate 1200→**1000**、night_action 1000→**600**（实测 298）、think 800→**600**。保留 1.5-2 倍余量。测试全过（test_agents+test_llm+test_orchestrator）、lint 0。⚠️ 收敛值是否仍清零 fallback 待下一局 live 实测确认（理论余量充足）。

- **max_tokens 调高 + 缓存/输出分析（用户两点指示）**：① max_tokens 再调高（speak/defense 1200、nominate 1200、nomination_intent **1500**、vote/night_action 1000、think 800），第三局实测 **fallback 清零**（0/21，真空输出 0），reasoning 实际占用：nomination_intent 738（completion 835 峰值，预算 1500 富余）、speak 268、night_action 218——模型不故意用满，高上限只提供余量不浪费；建议收敛值（可选）：nomination_intent→1200、speak/defense→1000、vote→800、night→600、think→600。② 缓存命中分析：**同 agent system 完全稳定**（每 Player 仅 2 版本：act 完整 2200 + reflect/协调辅助 1700，均为预期），全局层（层1）~1619 字符跨 agent 共享前缀，user1 稳定（38/38 相同），命中率 54-63%（已接近架构上限，对比优化前 12.7%）。优化方向：精简全局层可降低每次 miss 成本（受测试断言 >=1500 约束 + 前缀变动一次失效），未擅自做。③ 输出精简：reasoning 占 output 63.6%（DeepSeek thinking 固有），靠 AI_THINKING_LEVEL 降档或引导精简，未改 prompt。测试全过、lint 0。

- **Live 对局测试（用户要求）+ 修复 fallback 危机**：跑 5 人局 live（simulate_game + tmp_work/scripts/run_live_test.py，AI_FAST_LOW_VALUE_ACTIONS=0 让 vote/nomination 走 LLM）。**第一局（4天 game_over，323.7s）**：token 总 843,326（Input 797,051 hit 63.06% / Output 46,275 reasoning 35,474），计费≈0.44 CNY，**fallback 率 76.9%**（100 个真空输出：content+tool_calls 全空，finish=length）。根因=thinking 占满 max_tokens（speak=400/vote/night=200）无预算输出。**修复**：① max_tokens 调大（speak/defense 400→1000、vote 200→800、nomination_intent 200→1000、night_action 200→600、think 200→500）；② content 空时 `_extract_decision_from_reasoning` 从 reasoning_content 恢复决策 JSON（复用 OpenAIBackend._extract_json_objects）；③ **thoughts.jsonl 未落盘 bug**：`_append_player_thought_log` 原被 `if self.data_collector:` 守卫 + tool_calls 路径直接 return 不记录，已移出守卫并在 tool_decision 分支也调用。**第二局（day_1，104.4s）**：fallback 76.9%→**25%**（speak/night 归零），真空输出 100→**8**（全为 nomination_intent，reasoning 占满 600→已调 1000 待验证）。⚠️ token 成本高（2 局已耗 ~1M tokens），nomination_intent 思考特别长是残余问题。临时脚本：tmp_work/scripts/run_live_test.py + analyze_tokens.py（解析 llm.jsonl 输出 hit/miss/reasoning/真空统计），.gitignore 已加 tmp_work/。测试：test_agents+test_llm+test_orchestrator 全过、lint 0。

- **Scavenge 机制落地（用户要求，从 reasoning 块恢复 tool call）**：DeepSeek 开启 thinking 后偶发把 tool call JSON 写进 reasoning_content/thinking 块而非标准 tool_calls。实现：`action_tool_registry.py` 加 `known_tool_names()`（返回 `_TOOL_SCHEMAS` keys）；`openai_backend.py` 加 `_extract_json_objects`（平衡括号扫描，跳过字符串内括号）+ `_scavenge_tool_calls_from_text`（支持标准格式 `{"function":{"name","arguments"}}` 与简化格式 `{"name","arguments"}`，arguments 支持字符串再 load 或对象，按已知工具名过滤），在 `generate()` 标准 tool_calls 为空时从 reasoning_content→content 兜底 scavenge 并打 `[Scavenge]` 日志。测试：`test_openai_backend.py` 加 5 个 scavenge 单测。⚠️ `.env` 加 `AI_THINKING_LEVEL=medium` 后导致策略表测试失败（env 覆盖 casual 默认 off）——测试需 `monkeypatch.delenv("AI_THINKING_LEVEL")` 隔离，并新增 env override 测试用例。验证：test_llm+test_agents 全过、lint 0。

- **思考配置落地（用户三点要求）**：① `.env` 新增 `AI_THINKING_LEVEL=medium`（off/low/medium/high，覆盖难度预设，默认按难度分级 casual=off/standard=medium/master=high/chaos=high）。② DeepSeek 调用方式确认与官方示例一致（openai_backend.py:122-125：`thinking={"type":"enabled"}` 走 extra_body + `reasoning_effort` 透传），无需改动；官方示例模型 deepseek-v4-pro，本机 .env 仍用 v4-flash。③ 缓存命中：web_search 确认 DeepSeek 前缀缓存=服务端自动逐 token 字符串比较（prompt 首个 token 起，任一不一致后续全 miss，`prompt_cache_hit_tokens` 计费）；思考(reasoning)是模型输出不参与输入缓存。落地：`common_rules.py` 新增 `THINKING_GUIDANCE`（深度思考指导，纯静态）拼入 `build_global_static_layer()` 全局静态层，使思考指令成为稳定前缀。⚠️ 潜在风险：DeepSeek 常把 tool call JSON 写入 reasoning_content/thinking 块而非标准 tool_calls（openclaw issue 提及），live 实测若工具调用异常需加 scavenge 正则恢复机制。验证：test_agents 全过、lint 0。

- **两问题修复 + 未来共享经验池方向（用户需求）**：
  - ① 邪恶频道显示 JSON：根因=夜间协调消息（evil_strategy.py `generate_first_night_coordination` / `build_evil_night_coordination_message`）直接透传 LLM response.content，DeepSeek 偶发返回 JSON/markdown 包裹。修复：新增 `_extract_plain_message` 清洗（剥代码块 + JSON 提取 content/message/text），两处调用；前端 index.html 加 `extractPlainMessage` 兜底清洗。
  - ② 开启深度思考（用户决策：按难度分级 casual=off / standard=medium / master=high / chaos=high，env `AI_THINKING_LEVEL` 可覆盖）：`_llm_strategy_for_action` 从 classmethod 改 instance method，`_HIGH_VALUE_THINKING_ACTIONS`（speak/defense_speech/nominate/nomination_intent/vote/night_action/think）按 `_thinking_level()` 覆盖 thinking/reasoning_effort；reflect/archive/claim 保持 disabled。
  - 思考记录：`LLMResponse` 加 `reasoning_content` 字段（base_backend/openai_backend 提取完整不截断）；act() thought_trace 合并 `[深度思考]+[决策推理]`；新增 `_append_player_thought_log` 落盘 `data/agents/{pid}/games/{gid}/thoughts.jsonl`（**仅 live 后端落盘**，mock 跳过避免污染数据目录——测试曾产生 42 个测试 thoughts.jsonl 已清理）。
  - **未来设计方向（用户明确，待立项）**：所有 agent 共享"总结和经验记忆"——按每个单独玩家视角沉淀，经验统一学习；新对局 agent 依经验+技巧+自身人格设定/性格游玩，实现人类玩家体验到不同 AI 差异化的"活人感"。
  - 验证：pytest tests 全套通过、slow 验收 5 个通过、read_lints 0。改动：base_backend.py/openai_backend.py/ai_agent.py/evil_strategy.py/index.html/test_agent_tools.py。⚠️ DeepSeek live 实测需验证 thinking/reasoning_effort 参数兼容性（`AI_THINKING_LEVEL=off` 可回退）。测试 `test_llm_strategy_table_*` 更新为新分级断言（_make_strategy_agent helper）。

- **游戏结束无入口修复（用户实测：关闭结算卡片后主界面卡死无按钮）**：根因=结算卡片（settlementOverlay）上才有"再来一局"按钮，关闭后主界面 action-area 只剩正常游戏操作按钮。修复 `public/index.html` 三处：① action-area 新增 game_over 专属按钮 `rematchBtn`（🔄再来一局→requestRematch() 调 /api/game/rematch）和 `historyFromGameOverBtn`（📚历史对局→openHistoryFromSettlement()），初始 display:none；② `updatePhaseDisplay` 检测 phase==='game_over' 时显示两按钮、隐藏 actionInput/toneSelect/submitBtn/chatBtn/skipBtn/slayerBtn/voteSelect/evilChatToggleLabel/action-target-container；③ `clearSessionArtifacts`（新局开始）恢复正常控件+隐藏 game_over 按钮（恢复放这里而非 updatePhaseDisplay，避免与 handleActionRequest 按动作类型切换控件显示冲突）。静态文件改动，浏览器刷新即生效。lint 0。

- **无人提名问题修复（用户实测连续两天无人提名）**：根因①prompt 写死"怀疑度>0.60 才提名+不确信可直接不提名"引导 LLM 过度保守；②LLM 合法返回 action=none 直接放弃不触发兜底（decision_engine post_process 850 行）；③早期信号弱（target_signal_score 基础分 0.16）。已实现三修复：①prompt_factory 提名引导分天（day>=2 主动推动提名、day1 可观望但明显可疑要提名）+ action_hints 改为积极；②decision_engine post_process：LLM 返回 none 时先跑 `_select_nomination_target(intent_mode=...)`，有强信号目标则覆写为 nominate；③target_signal_score 加 `mention_boost` 参数——**仅提名路径**（_select_nomination_target 的 quick_scores/base_scores、nomination_candidate_band）用 0.08/cap0.30 提升早期信号敏感度，投票/其他决策保持 0.06/cap0.24。**踩坑**：最初直接改 target_signal_score 全局权重导致投票 suspicion 虚高、全员投 yes，4 个 slow 验收脚本挂 `aggressive_vote_push_rate should exceed silent_yes_rate: 1.0<=1.0`（aggressive/silent 人格 yes 率都=1.0），用 git stash 前后对比确认是改动引入；改 mention_boost 限定提名后恢复。验证：pytest tests 全套通过、read_lints 0、4 个 slow 验收测试通过。改动文件：prompt_factory.py / decision_engine.py。注意 replace_in_file 有一次报成功但内容未生效（312/317 行 quick_scores/base_scores boost 需事后 search 确认）。临时文件 _pytest_*.txt 待清理。

- **AI 拟人化 + 记忆体系梳理（用户问"能否根据前面人的话/行为调整策略 + 记忆如何设计读取"）**：拟人化响应分档——发言内容✅（public_fact_memory 实时写前120字，方案B精炼可见）、提名/投票行为✅（nomination_started/voting_resolved → objective_memory【绝对客观事实】）、死亡存活✅（visible_state players.is_alive）、夜间行为❌（信息隔离设计使然）、跨局经验✅（PlayerProfileStore 注入新局 system prompt）。记忆五层：①WorkingMemory（observations 30条 / internal_thoughts / impressions / MemoryTier 分层 OBJECTIVE-HIGH_CONFIDENCE-PUBLIC）；②EpisodicMemory（阶段归档 Episode，取8段）；③VectorMemory（embedding 检索 top_k=5，refinement_mode 跳过）；④SocialGraph（自报身份/信任度/怀疑）；⑤PlayerProfileStore 跨局进化（data/agents/{pid}/profile/{profile.json,long_term_memory,reflections,strategies,lessons,game_reviews}.jsonl + games/{game_id}/memory.jsonl 对局隔离，新局 build_long_term_summary 注入）。读取=决策时 act() prompt 组装（system 人格+跨局经验 + 分层摘要 cap 800/400/300 + 情节 + 社交图谱 + 局势摘要）；写入=被动 observe_event / 主动 MemoryTools（私密内容隔离校验）/ 反思 reflect 蒸馏 compact / 跨局 finalize_game_review 等。

- **公开信息可见性架构梳理（用户问"公开信息如何维护并保证所有agent可见"）**：① 真相源=不可变 GameState，`with_update/with_player_update/with_event/with_message` 迁移；公开字段分布：发言=chat_history+event_log、提名投票=nominations_today/nominees_today/votes_today/current_nominee/current_nominator、天数阶段=PhaseManager 状态机（VALID_TRANSITIONS 表）、存活=PlayerState.is_alive。② 双通道：按需查询=`InformationBroker.get_visible_state()` 实时构造 AgentVisibleState（players 只含 is_alive，不含 role/team）；事件推送=EventBus 订阅"*"（game_loop.py:108）→ broadcast_event 按 Visibility 路由（PUBLIC 全员 / TEAM_EVIL-GOOD / PRIVATE / STORYTELLER_ONLY）→ 并行 observe_event 写记忆。③ 分工：结构化状态走 AgentVisibleState→局势摘要；发言内容走事件广播→public_memories（方案B精炼可见性的基础）。广播同步 await 无时间差。

- **方案B 落地（白天发言动态响应）**：用户质疑 AI 白天发言全用预生成草稿原样发布、不响应场上其他玩家发言，确认属实。根因：`ai_agent.py act()` 486 行草稿短路直接复用（PLN-037 P0-4.3 优化），精炼逻辑（573-591 行"请基于最新局势修正草稿"）是 dead code。已实现方案B（用户选定）：① `act()` 短路条件加 `not refinement_mode`；② `day_discussion.py` 每轮记 `round_start_event_count`，轮到 AI 时若本轮已有人发言 → `refinement_mode=True` 走完整 LLM 精炼（注入草稿供修正），否则首位发言者直接复用草稿省一次 LLM；LLM 超时/失败仍用草稿兜底（735-774 行既有逻辑）。测试：新增 `test_day_discussion_draft_policy_reuse_first_then_refine` + `DraftTrackingAgent` 替身（`generate_draft_speech` 返回草稿 + 记录 speak kwargs）。验证：test_agents+test_orchestrator 357 全过、test_game_loop 27 全过、lint 0。改动需重启 server。

- **AI 发言可见性确认（用户问"前面AI会响应更先发言的AI吗"）**：会。链路：AI-A 发言 → publish(player_speaks) 阻塞 → broadcast_event（information_broker.py:240-242 asyncio.gather 同步）→ 各 AI observe_event → remember_critical_event 实时写 public_fact_memory（event_observer.py:474-484，`{actor_name}: {content[:120]}`）→ AI-B 精炼时 act() prompt 的【公开讨论与声明】包含它（ai_agent.py:552-559 最近15条）。限制：①发言只记前120字；②公开记忆只取最近15条；③首位发言者（本轮第一个开口）不精炼直接复用草稿。refinement_mode 跳过向量检索但不影响 public_fact_memory（非检索来源）。

- 用户边实测边反馈。**修复白天发言顺序**：原 `src/orchestrator/phases/day_discussion.py` 分两段执行（Step 3 所有人类先发言 → Step 4 所有 AI 再按座次），导致人类玩家无论座次总是最先发言。已合并为单一循环，严格按 `state.players`（= seat_order）顺序让人类与 AI 穿插发言，保留 `SpeechPreGenCache` 后台预生成优化（轮到 AI 时 `get_or_wait` 取草稿）；被 `ai_message_limit` throttled 的 AI 仍跳过。测试：`test_game_loop.py` 新增 `test_day_discussion_speaks_in_seat_order_when_human_in_middle`（人类在中间座位断言 `["p1","human","p2","p3"]`），lint 0、test_game_loop 26 用例全过。改动需重启 server 生效。

- 用户计划自行实测游戏启动。**配置结论（修正）**：`.env` 文件已完整配置 live 模式（DeepSeek）：`BOTC_BACKEND=live`、`OPENAI_API_KEY`、`OPENAI_BASE_URL=https://api.deepseek.com`、`DEFAULT_MODEL=deepseek-v4-flash`、`EMBEDDING_BASE_URL/MODEL`、`OPENAI_REQUEST_TIMEOUT_SECONDS=300`。代码启动时经 `load_dotenv()` 自动加载（`src/api/server.py`、`src/llm/openai_backend.py`、`simulate_game.py` 三处），无需手动 `$env:`。`.env` 已在 .gitignore 排除。注意：`load_dotenv()` 默认不覆盖已存在的 shell 环境变量。首次检查时误只查 shell/系统级变量（均为空）得错结论，后经用户提示发现 `.env`。
