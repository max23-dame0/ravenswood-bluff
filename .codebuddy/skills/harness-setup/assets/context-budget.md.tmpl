# 上下文预算配置

> 本文件声明 Agent 每次会话上下文窗口的分配策略。
> 来源：L04 渐进式披露 + Codex project_doc_max_bytes 实践

## 总量限制

| 项目 | 上限 | 说明 |
|------|:--:|------|
| 总预算 | {{CONTEXT_BUDGET}} | 默认 32768 字节 (32 KiB) |
| 入口文件 | 200 行 | 仅做路由器，不写细节 |
| MEMORY.md | 200 行 | 超出部分精简或拆分子文件 |
| 单个规则文件 | 80 行 | 过长规则拆分为子规则 |

## 分层加载策略

| 加载时机 | 文件 | 策略 |
|---------|------|------|
| 每次会话必加载 | 入口文件 + MEMORY.md | 全量 |
| 上班时加载 | PROGRESS.md + DECISIONS.md | 全量 |
| 按模块按需加载 | `.codebuddy/rules/` 匹配的规则 | 仅加载命中 glbo 的文件 |
| 涉及特定技术时加载 | `documents/05-reference/tech-traps.md` | 按需 |
| 会话结束时更新 | session-handoff.md | 写入 |

## 截断规则

当上下文接近预算上限时，按以下优先级释放空间：

1. MEMORY.md 的历史记忆条目（保留最近的）
2. 非匹配模块的规则文件（drop）
3. 专题文档中的历史示例（保留最新的一条）

## 退化告警

- 入口文件 > 200 行 → ⚠️ 警告：请拆分到 rules/ 或专题文档
- MEMORY.md > 200 行 → 🤖 AI 请自行精简历史记录
- 总加载量 > {{CONTEXT_BUDGET}} → 🔴 严重：请立即按截断规则释放空间
