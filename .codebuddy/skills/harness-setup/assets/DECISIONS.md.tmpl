# DECISIONS — 架构决策记录

> 记录关键设计决策及原因。**上班必读**，避免新会话推翻已有决定。
> 格式：日期 + 决策 + 原因 + 否决方案 + **回退/可逆方案** + 约束条件（L05 标准）
> 强调「可逆设计」：每条决策须写明若上线出问题如何回退（如保留旧链路做 fallback、灰度、开关），避免不可逆改动。

---

## D001: 项目 Harness 体系采用五子系统架构

- **日期**：{{DATE}}
- **决策**：采用 Harness Engineering 五子系统模型（指令+工具+环境+状态+反馈），通过入口文件/PROGRESS.md/MEMORY.md/DECISIONS.md 四个核心文件 + 分层规则建立 coding agent 工作环境
- **原因**：基于 L02-L12 课程实践，结构化的 harness 可将 Agent 任务成功率提升 45%，安全约束遵循率提升 35%
- **否决方案**：纯 Prompt 驱动（无结构化 harness 文件，上下文效率低、跨会话无法连续性）
- **回退/可逆方案**：harness 文件均为 Markdown，可随时 `git revert` 回退；若某规则误伤开发，删该行即可，无代码级风险
- **约束**：所有 Agent 会话必须遵守入口文件中的上班/下班/WIP 显式登记流程

## D002: 规则采用 MDC 风格分层管理

- **日期**：{{DATE}}
- **决策**：项目规则按照作用域（global → 模块 → 文件级）分层管理，每个规则文件使用 YAML frontmatter 声明生效条件（globs / alwaysApply）
- **原因**：避免一次性全量加载所有规则导致上下文溢出，同时支持 monorepo 场景下不同模块的差异化规则
- **否决方案**：所有规则写入单文件（上下文预算不可控，团队冲突难以解决）
- **约束**：规则文件 < 80 行，入口文件 < 200 行，MEMORY.md < 200 行

{{EXISTING_DECISIONS}}
