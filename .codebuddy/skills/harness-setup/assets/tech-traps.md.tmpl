# 关键技术陷阱

> **角色**：[Cold] 冷记忆 — 仅在涉及对应技术时按需加载
> **来源**：项目实战踩坑记录 | **过期条件**：项目升级对应依赖版本后重新评估每条
> **使用**：每新增一条陷阱，追加到下方。格式：触发条件 + 错误做法 + 正确做法

---

<!-- 每条陷阱用以下模板：
## N. {陷阱名称}

- **触发条件**：什么情况下会踩坑
- **错误做法**：错误的代码或操作
- **为什么错**：根因分析
- **正确做法**：正确的代码或操作
- **检测方法**：如何通过代码审查或工具检测
-->

## 示例陷阱（语言无关，请根据项目实际情况替换或删除）

### 1. 空值/空引用陷阱

- **触发条件**：调用可能返回 null/None/nil 的 API 或函数
- **错误做法**：直接对返回值链式调用属性或方法，不做空值检查
- **为什么错**：运行时抛出 NullPointerException / TypeError / panic，且异常点远离真正的空值来源
- **正确做法**：使用语言惯用的空安全机制（Optional / `?.` / `if let` / pattern matching），或尽早做 Guard Clause 检查
- **检测方法**：静态分析工具（lint/类型检查器）通常能检测潜在的 null dereference

### 2. 部署路径/环境差异陷阱

- **触发条件**：代码中硬编码了本地开发环境的绝对路径或配置
- **错误做法**：`const DATA_DIR = "/Users/me/project/data"` 直接写死在代码中
- **为什么错**：换一台机器或部署到容器/CI 环境时路径不存在，导致运行时错误
- **正确做法**：通过环境变量、配置文件或运行参数注入路径，代码中不出现任何绝对路径
- **检测方法**：代码审查时搜索 `/Users/`、`C:\`、`/home/` 等绝对路径前缀

{{ADDITIONAL_TRAPS}}
