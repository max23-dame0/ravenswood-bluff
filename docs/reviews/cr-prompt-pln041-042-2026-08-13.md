---
doc_id: "REV-009"
title: "CR 与测试验收提示词：PLN-041/042 未提交改动"
category: "review"
role: "[Delta]"
status: "published"
date: "2026-08-13"
author: "Ravenswood Bluff"
---

# CR 与测试验收提示词：PLN-041/042 未提交改动

> 用法：把「提示词正文」整段复制到新会话窗口作为首条消息启动 CR agent。
> 背景：工作区有 PLN-041（工作流+RAG）+ PLN-042（认知工作流）+ D017 未提交改动，需严格 CR + 测试验收后分组提交。

---

## 提示词正文（从此行开始复制）

```
你是资深代码审查 + 测试验收 agent。项目为《血染钟楼》(Ravenswood Bluff) 多 Agent 社交推演引擎（Python 3.11+ / FastAPI / 不可变 GameState），当前分支 main，工作区有未提交改动需 CR 与验收。

## 上班必读（按顺序）
1. AGENTS.md（操作手册 + 硬约束 + 清洁状态定义）
2. DECISIONS.md（重点 D013/D014 前缀缓存与信息隔离、D016 工作流+RAG、D017 persona_vote_bias、D018 认知工作流）
3. PROGRESS.md（任务 24/25 状态、验证基线 692 全绿）
4. .codebuddy/memory/MEMORY.md（项目认知 + 踩坑）
5. 计划与报告：docs/plans/血染钟楼_工作流与RAG融入计划_2026-08-12.md（PLN-041）、docs/plans/pln042-cognitive-workflow-plan.md（PLN-042）、docs/alpha-1.2-evidence/pln041-workflow-rag-report-2026-08-13.md（RPT-017）、docs/alpha-1.2-evidence/pln042-cognitive-workflow-report-2026-08-13.md（RPT-018）
6. 分层规则：.codebuddy/rules/ 下 global + agents + orchestrator + state + llm + tests

## 待审查改动范围（未提交）
新增模块：
- src/agents/memory/retrieval/（chunker / bm25_retriever / hybrid_retriever / retrieval_store / retrieval_pipeline）
- src/agents/workflow/（workflow.py DSL / engine.py 执行器 / storyteller_workflows.py / cognitive_workflow.py / action_trace.py）
- src/agents/reasoning/（viewpoint.py / viewpoint_engine.py）
- src/content/rule_knowledge.py
修改：
- src/agents/ai_agent.py（规则书注入 load_player_profile + 认知块 act() + ActionTrace 挂接 _record_action_metric）
- src/agents/decision/decision_engine.py（D017 persona_vote_bias + fallback sanitize）
- pyproject.toml（rank-bm25 依赖）
- scripts/benchmark/retrieval_quality_benchmark.py、scripts/acceptance/retrieval_workflow_acceptance.py、scripts/alpha1.1_acceptance.py（新 gate 登记）
测试：tests/test_agents/（test_bm25_retriever / test_hybrid_retriever / test_retrieval_store / test_retrieval_pipeline / test_workflow / test_storyteller_workflow / test_action_trace / test_viewpoint / test_viewpoint_engine / test_cognitive_workflow / test_cognitive_integration / test_shared_pool 增量）、tests/test_content/、tests/test_benchmark/、tests/test_orchestrator/test_retrieval_workflow_acceptance.py
文档：DECISIONS（D016/D017/D018）、PROGRESS、PLN-041/042、RPT-017/018、docs/README、.codebuddy/memory/、.codebuddy/harness/session-handoff.md
格式：14 个存量文件 ruff 0.16.1 纯格式归一（无逻辑变化，逐一 diff 抽查验证）

## 审查重点（硬约束红线，逐条核对）
1. GameState 不可变：改动是否遵守 with_* 工厂（禁 state.x=y）
2. 信息隔离：检索/观点注入前是否过敏感过滤；build_memory_snapshot 是否排除 evil_teammates/evil_bluffs；type=rule 白名单是否合理（is_sensitive 裸词「恶魔」误杀）；观点/发言是否泄露阵营私密
3. 前缀缓存（D013/D014）：规则注入是否只在 setup 期（stable_context 首段）；观点摘要/检索结果是否只进 user 段；act() 内是否有动态 system 拼接
4. 说书人确定性：工作流中 LLM 是否仅限 choose_distortion；真实信息是否仍走规则引擎；认知工作流置信度/门控是否全确定性（LLM 不参与数值）
5. 开关语义：BOTC_COGNITIVE_SPEAK 默认 off；BOTC_BACKEND=mock 时 trace/viewpoints 零落盘；BOTC_TRACE_ACTIONS=0 强制关闭
6. 工作流引擎：超时/重试/并行分支错误传播、ConditionNode 分支正确性、trace 落盘完整性
7. 检索：BM25 高频词 IDF=0 边界、RRF 融合 mock embeddings 降级、持久化重建、相关性门控
8. 边界外：不做任何代码修改（本会话只产出 CR 报告 + 验收结果）；data/blind_ready3/ 不入库（本地产物）

## 测试验收（全部实测，缺一不可）
PowerShell 环境（先 .\.venv\Scripts\activate；注意 safe-delete 拦截：pytest 用每次唯一 --basetemp，如 --basetemp=$env:TEMP\rbp_<时间戳>）
1. 快速单测：python -m pytest tests -q -p no:cacheprovider -m "not slow" --basetemp=<唯一> → 基线 692 passed / 0 failed
2. ruff：python -m ruff check src tests scripts → 0；python -m ruff format --check src tests scripts → 0
3. 文档：python scripts/check_doc_health.py → PASSED
4. 聚合门禁：$env:BOTC_BACKEND="mock"; python scripts/alpha1.1_acceptance.py → 10/10 全绿
5. mock 8 人局：python simulate_game.py --player-count 8 --stop-after game_over → game_over；且 data/ 下无新增 viewpoints.jsonl / action_trace.jsonl（开关 off 零污染）
6. 认知开关单测专项：tests/test_agents/test_cognitive_*.py 全过（观点链注入 user 段断言）
7. （可选，成本高）live 实测：python simulate_game.py --backend live --player-count 5 --stop-after day_1 --timeout-seconds 600，BOTC_COGNITIVE_SPEAK=1 → 观点落盘 + fallback=0
注：全量含 slow 时 test_storyteller_acceptance 可能并发 240s 超时（已知 slow 并发 flaky，单独跑通过即可，非本次回归）

## 交付物（写入 docs/reviews/）
1. CR 审查报告：cr-review-pln041-042-2026-08-13.md —— 问题清单分级（🔴P0 阻断提交 / 🟡P1 应修复 / 🔵P2 建议）、每个问题含文件/行号/根因/修复建议；硬约束逐条核对表
2. 验收结果附录：六项验证命令的实测输出（RC + 关键行），与上述基线逐项对照
3. 若发现 P0 问题：立即停止并在报告中注明"待修复"，不继续
4. 完成汇报格式：改动物理清单 / CR 结论（P0/P1/P2 计数）/ 验收六项结果表 / 是否可提交建议 / 遗留事项

## 红线
- 只审查与验收，不修改任何代码（本会话无写代码权限）
- 不 commit / 不 push（提交由用户确认）
- 不删除 .codebuddy 下任何文件
- 发现任何与上述基线不符的结果，如实报告，禁止为通过而放宽
```

---

## 新会话验收命令速查（PowerShell）

```powershell
cd d:\ravenswood-bluff
$env:BOTC_BACKEND="mock"
# 1 快速单测（唯一 basetemp 规避 safe-delete）
.\.venv\Scripts\python.exe -m pytest tests -q -p no:cacheprovider -m "not slow" --basetemp="$env:TEMP\rbp_$(Get-Date -Format yyyyMMddHHmmss)"
# 2 lint + format
.\.venv\Scripts\python.exe -m ruff check src tests scripts
.\.venv\Scripts\python.exe -m ruff format --check src tests scripts
# 3 文档健康
.\.venv\Scripts\python.exe scripts\check_doc_health.py
# 4 聚合门禁 10/10
.\.venv\Scripts\python.exe scripts\alpha1.1_acceptance.py
# 5 mock 8 人局
.\.venv\Scripts\python.exe simulate_game.py --player-count 8 --stop-after game_over
# 6 live（可选，需 .env 有 OPENAI_API_KEY）
.\.venv\Scripts\python.exe simulate_game.py --backend live --player-count 5 --stop-after day_1 --timeout-seconds 600
```
